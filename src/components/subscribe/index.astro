---
import { SubscribeForm } from "./subscribe-form";
---

{/* The desk pose travels inside the form, through `NewsletterForm`'s `aside`,
    so the panel lays out its own two columns instead of a hand-placed image. */}
<SubscribeForm client:visible />
